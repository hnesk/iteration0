{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content %}
{% if error %}
<div class="col-lg-12">
    <div class="alert alert-dismissable alert-danger">
        <button type="button" class="close" data-dismiss="alert">×</button>
        <h4>Fehler!</h4>
        <p>{{ error|trans({}, 'FOSUserBundle') }}.</p>
    </div>
</div>
{% endif %}

    <div class="col-lg-4">
        <div class="panel panel-default">
            <div class="panel-heading"><h3 class="panel-title"><strong>Login</strong></h3></div>
            <div class="panel-body">
                <form action="{{ path("fos_user_security_check") }}" method="post" role="form">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                    <div class="form-group">
                        <label for="login_username">{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}</label>
                        <input id="login_username" name="_username" class="form-control" placeholder="{{ 'security.login.username'|trans({}, 'FOSUserBundle') }}" type="text" value="{{ last_username }}" required="required" autofocus="">
                    </div>
                    <div class="form-group">
                        <a class="pull-right" href="{{ path('fos_user_resetting_request') }}">({{ 'resetting.email.subject'|trans({}, 'FOSUserBundle') }})</a>
                        <label for="login_password">{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}</label>
                        <input id="login_password" type="password" class="form-control" placeholder="{{ 'security.login.password'|trans({}, 'FOSUserBundle') }}" name="_password" required="required">
                    </div>
                    <div class="form-group">
                        <input type="checkbox" id="login_remember_me" name="_remember_me" value="on" />
                        <label for="login_remember_me">{{ 'security.login.remember_me'|trans({}, 'FOSUserBundle') }}</label>
                    </div>

                    <button type="submit" id="_submit" name="_submit" class="btn btn-primary">
                        {{ 'security.login.submit'|trans({}, 'FOSUserBundle') }}
                    </button>
                </form>
            </div>
        </div>
    </div>
{% endblock fos_user_content %}
